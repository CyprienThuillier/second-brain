Internet Control Message Protocol (ICMP) is mainly used for network diagnostics and error reporting using two popular commands :

- `ping`: This commands uses ICMP to test connectivity to a target
- `traceroute` (`tracert` on Windows): This command uses ICMP to discover the route from your host to the target 